# ALCERecipeViewer 1.0.9 更新公告

发布日期：2026-09-03

- 新增菜单自定义按钮，可配置左键、右键及 Shift 点击动作。
- 支持 `command:`、`op:`、`console:` 三种命令执行方式。
- 支持分号多命令、`delay`/`wait` 延时及玩家、UUID、世界占位符。
- 兼容原有 `RUN_COMMAND`、`command`、`as_player` 配置。
